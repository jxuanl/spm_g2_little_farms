rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes for testing/emulator environment
    // In production, these rules should be more restrictive
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
